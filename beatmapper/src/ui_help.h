#pragma once

#include "editor.h"

// Keyboard / mouse shortcut reference window.
// Visible while editor->show_help is set; the H key toggles it (main.cpp).
void ui_help_render(EditorState* editor);
